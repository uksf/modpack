class UKSF_Settings {
    class GVAR(ID) {
        category = "UKSF";
        displayName = QUOTE(Persistence ID);
        description = QUOTE(ID for persistence database. Keep this the same for campaigns.);
        typeName = "STRING";
        isClientSettable = 0;
        value = "DEFAULTWILLNOTLOAD";
    };
};
